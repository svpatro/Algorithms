#include <iostream>

int partition(){



}

void quicksort(){



}

int main(){

    int size = 10; // There are 10 elements in this array
    int* arr = new int[size];


    // Create 2 partitions in the array, sort them separately. No need to recombine. 

    // int q = partition(A, p, r);
    // quicksort(A, p, q - 1);
    // quicksort(A, q + 1, r);


    return 0;

}